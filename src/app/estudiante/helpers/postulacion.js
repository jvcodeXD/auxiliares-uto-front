import React from 'react'
import axios from 'axios'

export const postularMateria = (id) => {
    if (id.length > 0)
        //Peticion para postular materia    
        console.log('Postular auxiliatura: ', id)
}